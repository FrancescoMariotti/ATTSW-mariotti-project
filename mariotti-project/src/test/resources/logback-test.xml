 <configuration>
  <include
    resource="org/springframework/boot/logging/logback/base.xml" />

  <!-- Limit WebDriverManager logs -->
  <logger name="org.apache.http" level="WARN" />

  <!-- Enable debugging for classes -->
  <logger name="com.mariotti" level="debug" />

</configuration>